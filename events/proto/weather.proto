syntax = "proto3";

option csharp_namespace = "Sr.Grpc.gen";

enum WeatherType{
  UNKNOWN = 0;
  RAIN = 1;
  STORM = 2;
  SUN = 3;
  CLOUDS = 4;
  FOG = 5;
}

package weather;

message WeatherSubscription{
  string location = 1;
}

message WeatherEvent{
  string location = 1;
  WeatherType type = 2;
  float temperature = 3;
}

service WeatherSubscriber {
  rpc Subscribe(WeatherSubscription) returns (stream WeatherEvent);
}